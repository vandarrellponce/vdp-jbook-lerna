/* example adding module in lerna 
- lerna add [module] --scope=[target] 
- lerna add commander --scope=cli
*/

/* Adding typscript 

lerna add typescript --dev --scope=local-api

and then initialize a tsconfig.json file by 
npx typescript --init
*/
